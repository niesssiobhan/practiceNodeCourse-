'use strict';

const bcrypt =require('bcrypt');

// this function is going to put salt before and after your password. it makes it so that it is very complex to decode and get someones password
async function run() {
  console.log(salt);
  console.log(hashed);
}

run();
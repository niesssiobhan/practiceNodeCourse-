'use strict';

let _ = require('underscore'); // this recogizes that this is a node_module

let result = _.contains([1, 2, 3], 2); // these are the arguments that the underscore module takes in. and array and then something that may be contained with in the 
// this is also a third part node module tha is being used 
console.log(result);